interface:
  display_name: "ESDiag Release"
  short_description: "Prepare and verify an ESDiag release"
  default_prompt: "Use $esdiag-release to prepare and verify an ESDiag release through target-specific validation and explicit human publication gates."
