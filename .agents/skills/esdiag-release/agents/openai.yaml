interface:
  display_name: "ESDiag Release"
  short_description: "Prepare and publish an ESDiag release"
  default_prompt: "Use $esdiag-release to prepare an ESDiag release through target-specific verification and publication gates."
