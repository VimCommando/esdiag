Elastic Stack Assets
====================

These are the configuration assets for the other Elastic Stack components. These need to be installed prior to running `esdiag process` or importing through the web interface of `esdiag serve` for the first time.

These are used by `esdiag-control up` and `esdiag-control setup` when deploying Elastic Stack Diagnostics to a new cluster.
